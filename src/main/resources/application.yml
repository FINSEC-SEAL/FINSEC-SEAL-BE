spring:
  application:
    name: finsec-seal-be
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        type:
          json_format_mapper: com.finsecseal.config.ToolsJacksonJsonFormatMapper
        jdbc:
          time_zone: UTC
  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:true}
  jackson:
    deserialization:
      fail-on-unknown-properties: true
  mvc:
    problemdetails:
      enabled: true

server:
  shutdown: graceful
  forward-headers-strategy: framework

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

finsec:
  generation:
    worker-enabled: ${FINSEC_GENERATION_WORKER_ENABLED:true}
    poll-ms: ${FINSEC_GENERATION_POLL_MS:1000}
    lease-seconds: ${FINSEC_GENERATION_LEASE_SECONDS:180}
  contract-access:
    key: ${FINSEC_CONTRACT_ACCESS_KEY:}
    actor: ${FINSEC_CONTRACT_ACCESS_ACTOR:}
    workspace: ${FINSEC_CONTRACT_ACCESS_WORKSPACE:}
  scheduling:
    enabled: ${FINSEC_SCHEDULING_ENABLED:true}
  cors:
    allowed-origins: ${FINSEC_CORS_ALLOWED_ORIGINS}
  crypto:
    key-base64: ${FINSEC_DATA_ENCRYPTION_KEY_BASE64}
  idempotency:
    ttl: ${FINSEC_IDEMPOTENCY_TTL:24h}
    recovery-key: ${FINSEC_IDEMPOTENCY_RECOVERY_KEY:}
    instance-stale-after: ${FINSEC_IDEMPOTENCY_INSTANCE_STALE_AFTER:30s}
    instance-heartbeat-ms: ${FINSEC_IDEMPOTENCY_INSTANCE_HEARTBEAT_MS:5000}
  sse:
    timeout: 30m
    heartbeat: 15s
  ai:
    enabled: ${FINSEC_AI_ENABLED:false}
    base-url: ${FINSEC_AI_BASE_URL:http://localhost:8001}
    connect-timeout: ${FINSEC_AI_CONNECT_TIMEOUT:2s}
    request-timeout: ${FINSEC_AI_REQUEST_TIMEOUT:5s}
    max-steps: ${FINSEC_AI_MAX_STEPS:8}
